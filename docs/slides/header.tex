% ============================================================================
% header.tex — preamble for etr_divergence_slides.Rmd
% Loaded via the YAML `includes: in_header:` field.
%
% Build:
%   rmarkdown::render("etr_divergence_slides.Rmd")
% xelatex / lualatex preferred (metropolis theme uses Fira Sans).
% ============================================================================

\usepackage{booktabs}
\usepackage{amsmath, amssymb}
\usepackage{multirow}
\usepackage{array}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{tikz}

% Wong colorblind-safe palette (matches code/utils/globals.do).
\definecolor{wblue}{RGB}{0,114,178}        % statutory / framework anchor
\definecolor{wvermillion}{RGB}{213,94,0}   % Treasury / actual / China
\definecolor{wgreen}{RGB}{0,158,115}       % gap / Census / Mexico
\definecolor{wgray}{RGB}{120,120,120}      % neutral
\definecolor{tblnavy}{RGB}{0,114,178}      % alias kept for backwards compat
\definecolor{tblred}{RGB}{213,94,0}

% White background, plain frame titles.
\setbeamercolor{background canvas}{bg=white}
\setbeamercolor{normal text}{bg=white, fg=black}
\setbeamercolor{frametitle}{bg=white, fg=wblue}
\setbeamercolor{progress bar}{fg=wvermillion}
\setbeamercolor{title separator}{fg=wvermillion}

% Find figures both next to this file (logo) and in the repo's results dir.
\graphicspath{{./}{../../results/figures/}}

% Disable metropolis's top-right \logo slot; we render our own bottom-left strip.
\setbeamertemplate{logo}{}
\titlegraphic{}

% Bottom strip: Budget Lab logo (left) + page number (right). Implementing
% as a real footline (rather than a tikz background overlay) reserves the
% bottom 11mm so frame content auto-flows above it instead of overlapping.
\setbeamertemplate{footline}{%
  \leavevmode%
  \hbox{%
    \begin{beamercolorbox}[wd=\paperwidth, ht=11mm, dp=0pt, leftskip=4mm, rightskip=8mm]{footline}%
      \raisebox{1.5mm}{\includegraphics[height=8mm]{budget_lab_logo.png}}%
      \hfill
      \raisebox{4mm}{\usebeamerfont{page number in head/foot}\usebeamercolor[fg]{page number in head/foot}\insertframenumber}%
    \end{beamercolorbox}%
  }%
}

% Helper macros for inline tier and gap call-outs in slide body text.
\newcommand{\tier}[1]{\textbf{\color{wblue}#1}}
\newcommand{\gap}[1]{\textit{\color{wvermillion}#1}}
